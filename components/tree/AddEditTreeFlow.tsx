const AddEditTreeFlow = ({ isThankATree }: { isThankATree: boolean }) => {
  /*
        Step 1:
            if (isThankATree) { My tree doesn't have a location: Skip }
            LocationSelector
        Step 2:
            Tree Details (Species, diameter, height, name)
            if (location) { Google Street View Selector }
        Step 3:
            Does it have an image? Skip
            Add Image (to tree)
        Step 4: (if isThankATree)
            SponsorshipDisplayForm


    */
};

export default AddEditTreeFlow;
